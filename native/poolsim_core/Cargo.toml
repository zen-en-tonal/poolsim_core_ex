[package]
name = "poolsim_core"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
poolsim-core = "0.1.0"
rustler = { version = "0.37.3", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
